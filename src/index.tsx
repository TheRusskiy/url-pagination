import _useUrlPagination from './useUrlPagination';
import _usePagination from './usePagination';
import _calculatePage from './utils/calculatePage';
import _calculateOffset from './utils/calculateOffset';

export const useUrlPagination = _useUrlPagination;
export const usePagination = _usePagination;
export const calculatePage = _calculatePage;
export const calculateOffset = _calculateOffset;
